# linux-from-scratch
linux-from-scratch

```
archlinux:
    pacman -S base-devel

debian:
    apt install build-essential bison

make download-source
make build
make chroot
> cd /opt/build
> make build
> exit
make run
```
